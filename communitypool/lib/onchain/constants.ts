/** Default EVM networks for MVP lookups (Alchemy network enums). */
export const DEFAULT_EVM_NETWORKS = ["eth-mainnet", "eth-sepolia"] as const;

/** Balance + history cache TTL (seconds). */
export const ONCHAIN_CACHE_TTL_SECONDS = 90;

export const SNAPSHOT_VERSION = 1 as const;
